name: E2E Test Fixture
entities:
  Item:
    properties:
      - name
  Task:
    properties:
      - name
      - { name: done, type: boolean, default: false }
    seed:
      - { name: "Buy groceries" }
      - { name: "Write tests", done: true }
      - { name: "Deploy app" }
